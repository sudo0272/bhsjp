const mysql=require("mysql");const getMysqlConnectionData=require("../models/getMysqlConnectionData").getMysqlConnectionData;const connection=mysql.createConnection(getMysqlConnectionData());const jsStringEscape=require("js-string-escape");const encryptSha512=require("../models/encryptSha512").encryptSha512;const encryptAes256=require("../models/encryptAes256").encryptAes256;function doAccountExist(id,password,callback){connection.query("SELECT * FROM accounts WHERE `id`=? AND `password`=?",[encryptAes256(jsStringEscape(id)),encryptSha512(password)],(error,results,fields)=>{if(error){throw error}callback(results[0])})}module.exports={doAccountExist:doAccountExist};